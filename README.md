# AudioConverter Telegram Bot 🎧📄➡️🎵

Телеграм-бот на Python, который конвертирует текстовые сообщения и документы (PDF, DOCX, TXT) в MP3 аудиофайлы с использованием сервиса Google Text-to-Speech (gTTS).

## ✨ Возможности

*   **Конвертация текста:** Превращает любое текстовое сообщение в аудио MP3.
*   **Чтение документов:** Извлекает текст из файлов форматов `.pdf`, `.docx`, `.txt`.
*   **Выбор языка:** Поддержка русского (RU), английского (EN), немецкого (DE) и французского (FR) языков для озвучивания (через инлайн-кнопки).
*   **Бессерверный деплой:** Оптимизирован для работы в облачных функциях Yandex Cloud Functions.

## 💻 Используемые технологии

Проект разработан с использованием следующих основных инструментов и библиотек:

*   **Язык программирования:** Python 3.10+
*   **Фреймворк для бота:** `pyTelegramBotAPI` (telebot)
*   **Синтез речи:** `gTTS` (Google Text-to-Speech)
*   **Обработка файлов:** `PyPDF2`, `python-docx`
*   **Развертывание:** Yandex Cloud Functions
*   **Работа с окружением:** `os`, `json`, `logging`, `tempfile`

## ⚙️ Установка и запуск (Локально)

Для запуска бота локально вам потребуется Python 3.10+.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone github.com
    cd название_репозитория
    ```

2.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```
    *(Убедитесь, что в вашем файле `requirements.txt` перечислены все библиотеки из кода: `pyTelegramBotAPI`, `gTTS`, `PyPDF2`, `python-docx`)*

3.  **Настройте переменные окружения:**
    Создайте файл `.env` в корне проекта и добавьте ваш токен бота, полученный от @BotFather в Telegram:
    ```ini
    BOT_TOKEN=ВАШ_ТОКЕН
    ```

4.  **Запустите бота:**
    ```bash
    python main.py
    ```

## ☁️ Развертывание в Yandex Cloud Functions

Бот настроен для работы через Webhook в бессерверной среде. Точка входа для Yandex Cloud — функция `handler` в файле `main.py` (или `app.py`).

1.  **Подготовка:** Создайте функцию в Yandex Cloud.
2.  **Переменные окружения:** Установите переменную окружения `BOT_TOKEN` в настройках функции.
3.  **Триггер:** Настройте API Gateway для получения вебхуков от Telegram.

*Подробная инструкция по настройке Webhook и Yandex Cloud Functions доступна в официальной документации Яндекса.*

## 🤝 Авторы

*   **Elena** - Идея и разработка -https://github.com/vita6719 

---
